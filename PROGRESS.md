# PROGRESS.md — 作業記録(新しい記録を上に追記する)

作業のたびに、この見出しのすぐ下へ「日時(日本時間)・やったこと・次にやること」を短く追記する。
次の起動はこのファイルの先頭を読んで続きから再開する。

---

## 2026-08-29 連続記録の節目(1週間・1か月など)にそっとお祝いを添える

- INBOX は空(司令塔からの新しい指示なし)。WORKING を維持して続行。
- やったこと(外部依存なし・安全な作業):
  - 連続記録がちょうど節目の日数(3・7・14・30・50・100・200・365日)に届いた日だけ、
    「1週間つづきました」等のお祝いを1行そっと添える(小さなキラッ ✨ アイコン付き)。
    毎日は出さず「届いた日」に一度だけ出す控えめな体裁。前回の「◯日つづけて書いています」の
    真下に、主色・小さめ文字で表示。習慣化のごほうびを仰々しくならないように。
  - ロジックは純粋関数に追加(lib/src/logic/streak.dart):
    `streakMilestoneLabel(days)`。節目でない日・0日以下は空文字=非表示。
  - カレンダーのフッターに節目行を追加(節目でなければ従来どおり見出しのみ)。
  - テスト追加: streak_test.dart に streakMilestoneLabel(節目・非節目・0以下)、
    widget_test.dart に「7日連続で1週間のお祝いが出る/2日連続では出ない」の表示テスト。
- 検証: 一時取得した Flutter 3.47.2(Dart 3.13.2)でローカル実行 →
  `flutter analyze` 問題なし、`flutter test` 100件すべて合格。CI(test.yml)でも再検証される。
- 次にやる予定(外部依存なし): カレンダー・画面遷移の微アニメーション調整、
  検索のヒント文言など細かな体裁改善。
- 判断待ち(お金・Macが必要=着手前に RED- で確認): 課金の本実装(A案で当面なし)、
  実機検証(起動3秒・機内モード)、アプリアイコン/起動画面、ストア用スクリーンショット。

---

## 2026-08-29 連続記録日数(◯日つづけて書いています)をホームに表示

- INBOX は空(司令塔からの新しい指示なし)。WORKING を維持して続行。
- やったこと(外部依存なし・安全な作業):
  - カレンダー画面のフッターに「連続で書いている日数」をそっと表示。
    2日以上つづいたときだけ「3日つづけて書いています」と主色で1行出す
    (毎日書く動機づけ。1日・0日は「連続」と呼ぶほどでないので非表示)。
  - 「今日はまだ書いていなくても、昨日までの連続は途切れとみなさない」設計。
    ホームを朝に開いても記録が消えて見えないようにした。
  - ロジックは純粋関数に分離(lib/src/logic/streak.dart):
    `currentStreakDays(entryDates, today)` と `streakLabel(days)`。
    AppState に `currentStreak([now])` を追加(基準日を渡せてテスト可能)。
  - テスト追加: streak_test.dart(連続・今日未記入・途切れ・重複・時刻無視・
    見出し文言)、app_state_test.dart に currentStreak、
    widget_test.dart にフッター見出しの表示/非表示。
- 検証: 一時取得した Flutter(stable)でローカル実行 →
  `flutter analyze` 問題なし、`flutter test` 95件すべて合格。CI(test.yml)でも再検証される。
- 次にやる予定(外部依存なし): カレンダー・画面遷移の微アニメーション調整、
  検索のヒント文言など細かな体裁改善。
- 判断待ち(お金・Macが必要=着手前に RED- で確認): 課金の本実装(A案で当面なし)、
  実機検証(起動3秒・機内モード)、アプリアイコン/起動画面、ストア用スクリーンショット。

---

## 2026-08-28 検索の空状態に探した語を添える

- INBOX は空(司令塔からの新しい指示なし)。WORKING を維持して続行。
- やったこと(外部依存なし・安全な作業):
  - 検索で一致0件のとき、これまでの「見つかりませんでした」に代えて
    「『雪』に一致する日記はありません」と探した語を添えて表示。
    何に一致しなかったか一目で分かり、打ち間違いにも気づきやすくした。
    長い語でも折り返せるよう中央寄せ+左右余白にした。
  - 文言は純粋関数に分離(lib/src/logic/search_label.dart):
    `searchEmptyLabel(query)`。前後空白を除き、空文字なら従来の一般文言を返す。
  - テスト追加: search_label_test.dart(語添え・空白除去・空文字の3ケース)、
    screens_widget_test.dart の空状態テストを新文言に更新。
- 検証: 一時取得した Flutter(stable)でローカル実行 →
  `flutter analyze` 問題なし、`flutter test` 83件すべて合格。CI(test.yml)でも再検証される。
- 次にやる予定(外部依存なし): カレンダー・画面遷移の微アニメーション調整、
  検索のヒント文言など細かな体裁改善。
- 判断待ち(お金・Macが必要=着手前に RED- で確認): 課金の本実装(A案で当面なし)、
  実機検証(起動3秒・機内モード)、アプリアイコン/起動画面、ストア用スクリーンショット。

---

## 2026-08-27 検索結果に件数見出しを追加

- INBOX は空(司令塔からの新しい指示なし)。WORKING を維持して続行。
- やったこと(外部依存なし・安全な作業):
  - 検索画面で結果があるとき、リストの上に「N件見つかりました」の見出しを表示。
    ヒント色・小さめ文字の控えめな体裁で、何件当たったか一目で分かるようにした。
  - 文言は純粋関数に分離(lib/src/logic/search_label.dart):
    `searchResultCountLabel(count)`。画面から切り出しテスト可能にした。
  - テスト追加: search_label_test.dart(件数の文言・0件でも組み立てる)、
    screens_widget_test.dart に「桜」で2件のとき見出しが出る widget テスト。
- 検証: 一時取得した Flutter 3.47.2(Dart 3.13.2)でローカル実行 →
  `flutter analyze` 問題なし、`flutter test` 80件すべて合格。CI(test.yml)でも再検証される。
- 次にやる予定(外部依存なし): カレンダー・画面遷移の微アニメーション調整、
  検索の空状態の言い回し調整など細かな体裁改善。
- 判断待ち(お金・Macが必要=着手前に RED- で確認): 課金の本実装(A案で当面なし)、
  実機検証(起動3秒・機内モード)、アプリアイコン/起動画面、ストア用スクリーンショット。

---

## 2026-08-27 書き出しテキストに見出し(期間・件数)を付与

- INBOX は空(司令塔からの新しい指示なし)。WORKING を維持して続行。
- やったこと(外部依存なし・安全な作業):
  - 書き出しテキストの先頭に見出し1行を追加。
    形式は「ひとこと日記(全期間・12件)」で、対象期間ラベルと件数が一目で分かる。
    バックアップ先(メモ・メール等)で中身が判別しやすくなる。
  - ロジックを純粋関数に追加(lib/src/logic/export_range.dart):
    `exportHeaderLine(range, count)` と `buildExportText(entries, range)`。
    空のときは見出しも付けず空文字を返し、「日記がありません」表示を保つ。
  - AppState.exportAsText を整理。全期間も絞り込みも同じ土台(キャッシュ=allEntries)から
    `buildExportText` で組み立てるよう統一(従来は全期間だけリポジトリ直呼び)。
    体裁が全期間・絞り込みで揃い、見出しも一貫して付く。
  - テスト追加: export_range_test.dart に見出し・組み立て・件数・空文字のケース。
- 検証: 一時取得した Flutter SDK でローカル実行 → `flutter analyze` 問題なし、
  `flutter test` 77件すべて合格。CI(test.yml)でも再検証される。
- 次にやる予定(外部依存なし): カレンダー・画面遷移の微アニメーション調整、
  検索画面の空状態/件数表示の体裁改善など。
- 判断待ち(お金・Macが必要=着手前に RED- で確認): 課金の本実装(A案で当面なし)、
  実機検証(起動3秒・機内モード)、アプリアイコン/起動画面、ストア用スクリーンショット。

---

## 2026-08-27 書き出しの日付範囲選択を実装(新方式での初回)

- 司令塔の指示(INBOX)を反映し、INBOX を空にした。運用は新方式(公開ソース置き場・
  報告は .factory/outbox/)に移行済み。移行後の再開報告を outbox に1件置いた。
- やったこと(外部依存なし・安全な作業):
  - テキスト書き出し画面に「全期間 / 今年 / 今月」の期間選択チップを追加。
    選んだ期間でコピー・共有・画面表示が絞り込まれる。
  - ロジックは純粋関数に分離(lib/src/logic/export_range.dart):
    `filterEntriesByRange`(基準日で期間フィルタ)/`formatEntriesAsText`(整形)。
    基準日を引数にしてテストで固定できるようにした。
  - AppState.exportAsText に range/now 引数を追加(既定は全期間=従来どおり)。
  - 空のときの案内を期間に応じて出し分け(全期間=「まだ日記がありません」/
    絞り込み=「この期間の日記はありません」)。
  - テスト追加: test/export_range_test.dart(フィルタ・整形・ラベル)、
    書き出し画面のチップ表示と「今月で対象外」の挙動。
- 検証: このサンドボックスには Flutter が無いため、一時取得した SDK で
  analyze / test をローカル実行して確認(結果は下記コミットで反映)。CI(test.yml)でも再検証される。
- 次にやる予定(外部依存なし): カレンダー・画面遷移の微アニメーション調整、
  書き出しの「期間ラベルを本文の先頭に付ける」等の細かな体裁改善。
- 判断待ち(お金・Macが必要=着手前に RED- で確認): 課金の本実装(A案で当面なし)、
  実機検証(起動3秒・機内モード)、アプリアイコン/起動画面、ストア用スクリーンショット。

---

## 2026-08-27 司令塔より: イシュー方式からの移行(これまでの要約)

これ以前の詳しい記録は司令塔 app-factory の archive/app-hitokoto-nikki-issue1.md に退避済み。

- 実装済み: カレンダー(書いた日が色づく)/1行エディタ(自動保存・前後日移動・未来日OK・空で削除)/
  検索(一致語のハイライト付き)/テキスト書き出し(コピー+共有シート share_plus)/
  無料枠30日+購入導線(課金は抽象+フェイクまで)/端末内JSON永続化/和トーンのテーマ(ライト・ダーク)
- 品質: flutter analyze 警告ゼロ / flutter test 64件すべて合格(2026-08-25時点)
- オーナーの決定: 課金はA案(無料機能で先行)/share_plus 追加OK(実装済み)/安全な作業は継続
- 残っていること(お金・Macが必要=着手前にRED-で確認): 課金の本実装(A案により当面なし)、
  実機検証(起動3秒・機内モード)、アプリアイコン/起動画面、ストア用スクリーンショット
- 次にやる予定(外部依存なし): 書き出しの日付範囲選択(全期間/この月 など)、
  カレンダー・画面遷移の微アニメーション調整
