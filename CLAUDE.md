# CLAUDE.md — アプリ用リポジトリの指示書(雛形)

このリポジトリは1つのアプリ専用。司令塔(app-factory)の CLAUDE.md と docs/ を親ルールとして必ず守る。

**このリポジトリは公開されている(ソース置き場)。** イシュー機能は使わない。やりとり(進捗の節目・判断待ち・オーナーの決定)はすべて司令塔の管理イシューで行う。**秘密の鍵や個人情報は絶対にここへ書かない。**

## このアプリの情報(リポジトリ作成時に記入)

- 名前: ひとこと日記(仮)
- 一言でどんなアプリ: 1日1行書くだけで、カレンダーに思い出がたまっていく日記アプリ
- 価格: 基本無料(30日分まで)+ 買い切り500円で無制限・全機能
- 管理イシュー: app-factory#3(報告はすべてここへ)

## 作り方

- **Flutter**で開発する(1つのコードでiPhoneと将来のAndroidに対応・無料)
- オフライン完結・サーバーなし。AIを使う場合は端末内で動く小型モデルのみ
- 見た目: 既製の標準部品そのままにせず、配色・余白・文字を整えて「おしゃれ」と言える水準にする。雛形っぽさを消すことは審査対策(store-safety.md)でもある
- ループエンジニアリング: 実装 → コードの自動チェックとテスト(Linux) → 修正、をすべて合格するまで繰り返す
- 1回の作業で欲張らず、区切りごとに PROGRESS.md へ記録する(中断されても続きから再開できるように)

## 完成の定義(全部満たすまで「完成」と言わない)

- [ ] 自動テストがすべて合格している
- [ ] 初回起動が3秒以内
- [ ] 機内モード(オフライン)で全機能が動く
- [ ] データがアプリを再起動しても消えない
- [ ] 課金がある場合、購入と復元(機種変更後の引き継ぎ)が動く
- [ ] 文言に誇張や誤りがない

## ビルドと申請

- Macマシンは release の自動実行からのみ使う(テスト版配布と本番申請の2場面だけ)
- 日々の確認はLinuxの自動テストで済ませる
- ストア掲載文・スクリーンショットは `store/` に置き、誇張禁止(store-safety.md)

## 作業記録と状況報告

- **PROGRESS.md**(作業記録): 作業のたびに先頭へ「日時・やったこと・次にやること」を追記する。
  次の起動はこのファイルの先頭を読んで続きから再開する
- **status.json**: 節目ごとに更新する(項目は operations.md 参照)
- **司令塔への報告(.factory/outbox/)**: 大きな節目・判断待ち・完成のときだけ、outbox に
  報告ファイルを置く(方法は自動実行 claude.yml の手順5のとおり。判断が要るものはファイル名を `RED-` で始める)。
  司令塔が毎日取りに来て管理イシューへ転記する。毎回の小さな進捗はPROGRESS.mdに書くだけでよい
- **司令塔からの指示(.factory/INBOX.md)**: オーナーの決定などはこのファイルに書かれて届く。反映したら空にする
- テスト依頼は司令塔の管理イシューのサブイシューとして起票される(書式は issue-templates.md ②)
- 秘密の鍵はSecretsのみ。コード・記録・ログに書かない(このリポジトリに司令塔へ書き込む鍵は置かない)

## 自走ループ(止まらないための決まり)

- 定時(日本時間 12時台・18時台・23時台)に自動で目を覚まし、`.factory/WORKING` ファイルがあれば、誰の指示も待たずに続きから作業する
- だから、**続きがある限り `.factory/WORKING` を置いたままにする**。消すのは「全部が判断待ちで塞がったとき」と「完成したとき」だけ
- 判断待ちが1つあっても、関係ない作業は止めない。「進めてよいですか?」と聞かず、進めてから報告する

## オーナーの判断が要るとき(大事)

- **オーナーはこのリポジトリを見ない。** ここに何を書いても届かない
- 判断が要るときは、自動実行 claude.yml の手順5に従い、`.factory/outbox/` に `RED-` で始まる
  報告ファイルを置く(司令塔が取りに来て、管理イシューに🔴付きで転記する)
- オーナーの回答は、司令塔が `.factory/INBOX.md` に書き込んだうえで起こしに来る
- 待っている間も、判断が要らない作業があれば止まらずに続ける
