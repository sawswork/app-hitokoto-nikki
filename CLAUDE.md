# CLAUDE.md — アプリ用リポジトリの指示書(雛形)

このリポジトリは1つのアプリ専用。司令塔(app-factory)の CLAUDE.md と docs/ を親ルールとして必ず守る。

## このアプリの情報(リポジトリ作成時に記入)

- 名前:
- 一言でどんなアプリ:
- 価格:
- 管理イシュー: app-factory#〇〇(報告はすべてここへ)

## 作り方

- **Flutter**で開発する(1つのコードでiPhoneと将来のAndroidに対応・無料)
- オフライン完結・サーバーなし。AIを使う場合は端末内で動く小型モデルのみ
- 見た目: 既製の標準部品そのままにせず、配色・余白・文字を整えて「おしゃれ」と言える水準にする。雛形っぽさを消すことは審査対策(store-safety.md)でもある
- ループエンジニアリング: 実装 → コードの自動チェックとテスト(Linux) → 修正、をすべて合格するまで繰り返す
- 1回の作業で欲張らず、区切りごとに進捗を管理イシューへ一言コメントする(中断されても続きから再開できるように)

## 完成の定義(全部満たすまで「完成」と言わない)

- [ ] 自動テストがすべて合格している
- [ ] 初回起動が3秒以内
- [ ] 機内モード(オフライン)で全機能が動く
- [ ] データがアプリを再起動しても消えない
- [ ] 課金がある場合、購入と復元(機種変更後の引き継ぎ)が動く
- [ ] 文言に誇張や誤りがない

## ビルドと申請

- Macマシンは release の自動実行からのみ使う(テスト版配布と本番申請の2場面だけ。10倍換算のため)
- 日々の確認はLinuxの自動テストで済ませる
- ストア掲載文・スクリーンショットは `store/` に置き、誇張禁止(store-safety.md)

## 状況報告

- 節目ごとに `status.json` を更新する(項目は operations.md 参照)
- テスト依頼は管理イシューのサブイシューとしてチェックボックス形式で起票する(書式は issue-templates.md ②)
- 秘密の鍵はSecretsのみ。コード・イシュー・ログに書かない
